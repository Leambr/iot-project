class Automation {

    constructor(id, sensor_id, type, task_date) {

        this.id = id;
        this.sensor_id = sensor_id;
        this.is_done = is_done;
        this.task_date = task_date;
        this.type = type;
    }
}

module.exports = Automation;